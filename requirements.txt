# Runtime dependencies.
# Qt 6.9 provides the unified macOS title bar and safe-area support.
PySide6>=6.9,<7
psutil>=5.9,<8

# YouTube/video site downloads from the GUI and CLI.
yt-dlp>=2025.1,<2027

# Document/PDF text extraction and higher-quality PDF export.
defusedxml>=0.7,<1
pypdf>=5,<7
reportlab>=4.2,<5

# Optional cloud uploads use the external rclone executable.
# Install and configure rclone separately when Cloud integration is enabled.
