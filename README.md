````md
# Media Converter GUI (Фото + Відео) — FFmpeg + Python (Tkinter)

Графічна програма на Python для пакетної конвертації **відео** та **зображень** через **FFmpeg**.

✅ Підтримує:
- конвертацію **відео** (MOV/MP4/MKV/WEBM/AVI/… → MP4/MKV/WEBM/MOV/AVI/GIF)
- конвертацію **фото** (JPG/PNG/WEBP/BMP/TIFF/… → JPG/PNG/WEBP/BMP/TIFF)
- автоматичне визначення типу файлу (**відео чи фото**) за розширенням
- вибір вихідного формату **окремо для відео і фото**
- нумерований список файлів у черзі (`1. 🎬 video.mov`, `2. 🖼️ photo.png`)
- пакетну обробку папки або вибраних файлів
- **“вертикалізацію” відео** (з горизонтального зробити вертикальне 9:16) двома способами:
  - **crop** (обрізає по центру)
  - **blur** (робить розмитий фон і вставляє відео по центру)
- лог + прогрес, конвертація в окремому потоці (UI не зависає)
- ручний вибір `ffmpeg.exe`, якщо FFmpeg не в PATH

---

## 📦 Вимоги

- **Python 3.10+**
- **FFmpeg**

Перевірка FFmpeg:
```bash
ffmpeg -version
````

---

## 🔧 Встановлення FFmpeg (Windows)

### Варіант 1: через winget

Відкрий PowerShell і виконай:

```powershell
winget install -e --id Gyan.FFmpeg
```

Після встановлення відкрий новий термінал і перевір:

```powershell
ffmpeg -version
```

### Варіант 2: вручну (без PATH)

1. Скачай FFmpeg zip-архів під Windows (build).
2. Розпакуй.
3. Знайди файл `bin\ffmpeg.exe`
4. Або:

   * додай `...\bin` в **PATH**, або
   * у програмі натисни **“Вказати ffmpeg.exe”** і вибери файл вручну.

---

## ▶️ Запуск

1. Збережи файл програми, наприклад:

* `media_converter_gui.py`

2. Запусти:

```bash
python media_converter_gui.py
```

---

## 🧭 Як користуватися

1. Запусти програму.
2. Якщо FFmpeg не знайдено:

   * натисни **“Вказати ffmpeg.exe”** і вибери файл, або встанови FFmpeg у PATH.
3. Додай файли:

   * **“Вибрати папку”** (додає всі файли з папки і підпапок)
   * **“Вибрати файли”** (можна багато)
4. Вкажи папку виводу.
5. Вибери:

   * формат для **відео**
   * формат для **фото**
   * (опційно) режим **вертикального відео**
6. Натисни **“Старт”**.

---

## 📱 Вертикальні відео (9:16)

У блоці налаштувань є пункт:

**“Зробити вертикальним”**
Працює **лише для відео**.

Доступно:

* `9:16 (1080x1920) — crop`
* `9:16 (1080x1920) — blur`
* `9:16 (720x1280) — crop`
* `9:16 (720x1280) — blur`

**crop** — обрізає по центру (підходить, якщо об’єкт у центрі кадру).
**blur** — робить розмитий фон, щоб нічого не обрізати (підходить для TikTok/Reels/Shorts).

> Важливо: при використанні “вертикалізації” опція **Fast copy** (без перекодування) не застосовується, бо потрібні фільтри.

---

## ⚙️ Поради по якості (відео)

* **CRF 18–23**: хороший баланс якості/розміру
* **Preset**:

  * `fast/medium` — нормально
  * `slow` — менший розмір, але довше
* **Fast copy**:

  * швидко, але спрацює не завжди (залежить від кодеків і контейнера)

---

## 🧩 Підтримувані формати

### Вхідні відео

`mov, mp4, mkv, webm, avi, m4v, flv, wmv, mts, m2ts`

### Вхідні зображення

`jpg, jpeg, png, webp, bmp, tif, tiff, heic, heif`

### Вихідні формати

* Відео: `mp4, mkv, webm, mov, avi, gif`
* Фото: `jpg, png, webp, bmp, tiff`

---

## ❗ Типові проблеми

### “FFmpeg не знайдено”

* Встанови FFmpeg у PATH або натисни **“Вказати ffmpeg.exe”** і вибери файл вручну.

Перевір у PowerShell:

```powershell
where ffmpeg
ffmpeg -version
```

### “Програма не бачить ffmpeg, але він встановлений”

* Відкрий **новий** термінал після встановлення
* або вкажи `ffmpeg.exe` вручну кнопкою

---

## 🗂️ Структура (приклад)

```
project/
  media_converter_gui.py
  README.md
  ffmpeg.exe          (опційно — можна покласти поруч)
  ffprobe.exe         (опційно)
```

---

## ✅ Ліцензія

Використовуй вільно у своїх проєктах.

```
::contentReference[oaicite:0]{index=0}
```
